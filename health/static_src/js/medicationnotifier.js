/* Baby Buddy Medication Notifier
 *
 * Handles creation and triggering of notifications about medication schedules.
 */
BabyBuddy.MedicationNotifier = function ($) {
    var MedicationNotifier = {
    };
    return MedicationNotifier;
}(django.jQuery);
